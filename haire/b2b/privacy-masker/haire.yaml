# ==============================================================================
# hAIre Agent Configuration v1.1
# ==============================================================================

# 1. Identity
info:
  slug: "privacy-masker"
  name: "개인정보 마스킹 에이전트"
  version: "1.0.0"
  description: "문서 속 개인정보를 자동으로 탐지하고 마스킹 처리하여 안전한 문서를 생성합니다."
  price: 800
  icon: "🛡️"

# 2. Runtime
run:
  engine: "python-3.11"
  entry_point: "privacy_masker.main:main"
  dependencies: "requirements.txt"

# 3. Resources
resources:
  llm:
    provider: "openai"
    model: "gpt-4o"
    parameters:
      temperature: 0.0
      max_tokens: 4096

  tools:
    - name: "presidio_analyzer"
      provider: "managed"

  auth: []

# 4. Inputs
inputs:
  - name: "document_text"
    type: "text_area"
    label: "마스킹할 문서 내용"
    placeholder: "마스킹 처리할 문서의 내용을 붙여넣으세요."
    required: true
    examples:
      - "계약서 제5조: 수급인 홍길동(주민등록번호 900101-1234567)은 본 계약의 이행을 위해 010-1234-5678로 연락 가능하며, 서울특별시 강남구 테헤란로 123에 거주한다."
      - "상담 로그 #2847: 고객 김영희(myemail@gmail.com)님이 계좌번호 110-123-456789로 환불을 요청하셨습니다."

  - name: "masking_targets"
    type: "select"
    label: "마스킹 대상 범위"
    options:
      - "전체 (이름, 주민번호, 연락처, 이메일, 주소, 계좌번호 등 모든 개인정보)"
      - "식별정보만 (이름, 주민번호, 여권번호, 운전면허번호)"
      - "연락정보만 (전화번호, 이메일, 주소)"
      - "금융정보만 (계좌번호, 카드번호)"

  - name: "masking_style"
    type: "select"
    label: "마스킹 방식"
    options:
      - "별표 처리 (홍** / 010-****-5678)"
      - "태그 대체 ([이름], [전화번호])"
      - "완전 삭제 (공란 처리)"

  - name: "include_report"
    type: "boolean"
    label: "탐지 결과 리포트 포함"

# 5. Outputs
outputs:
  view_type: "markdown"
